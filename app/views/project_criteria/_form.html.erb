<%= form_with(model: project_criterium, local: true) do |form| %>
  <% if project_criterium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project_criterium.errors.count, "error") %> prohibited this project_criterium from being saved:</h2>

      <ul>
      <% project_criterium.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :project_criterium_name %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value, id: :project_criterium_value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
